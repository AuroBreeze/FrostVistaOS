[package]
name = "frostvista-rust"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "frostvista_rust"
crate-type = ["staticlib"]
test = false
bench = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
debug = 2
